# ── Core data & ML ────────────────────────────────────────────
numpy>=1.26.4
pandas>=2.2.2
scikit-learn>=1.6.0
joblib>=1.4.2

# ── Deep learning — CPU only (no CUDA, avoids huge GPU downloads) ─
# torch is installed separately in the workflow via --index-url
# so do NOT put torch here — it is handled by the workflow yml

# ── Dashboard & API ───────────────────────────────────────────
streamlit>=1.35.0
flask>=3.0.3
gunicorn
plotly>=5.22.0

# ── Data fetching ─────────────────────────────────────────────
requests>=2.32.3
python-dotenv>=1.0.1

# ── Explainability & visualisation ────────────────────────────
shap>=0.46.0
matplotlib>=3.9.0

# ── MLOps / Feature Store ─────────────────────────────────────
hopsworks==5.0.4
confluent-kafka>=2.3.0